global:
  resolve_timeout: 5m

route:
  receiver: default
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: default
    webhook_configs:
      - url: http://monitor:8080/internal/alert
        send_resolved: true
